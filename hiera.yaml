---
version: 5

defaults:
  datadir: "data"
  data_hash: yaml_data

hierarchy:
  - name: "Node specific"
    path: "%{hostname}.yaml"
  - name: "OS Family specific"
    path: "%{osfamily}.yaml"
  - name: "Common values"
    path: "common.yaml"
