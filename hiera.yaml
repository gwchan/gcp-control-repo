---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Node specific"
    data_hash: yaml_data
    path: "%{hostname}.yaml"

  - name: "OS Family specific"
    data_hash: yaml_data
    path: "%{osfamily}.yaml"
